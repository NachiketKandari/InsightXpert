services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: insightxpert
      POSTGRES_PASSWORD: insightxpert
      POSTGRES_DB: insightxpert
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
